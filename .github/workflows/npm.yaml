name: hello

on:
  push:

jobs:
  my_first_job:
    name: My first job
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: install
        run: |
          cd npm
          npm ci
  # my_second_job:
  #   name: My second job
  #   runs-on: ubuntu-latest
